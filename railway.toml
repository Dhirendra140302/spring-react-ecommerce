[build]
builder = "NIXPACKS"

[deploy]
startCommand = "java -Dserver.port=$PORT -jar ecommerce_backend/target/ecommerce-0.0.1-SNAPSHOT.jar"
healthcheckPath = "/api/products"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
